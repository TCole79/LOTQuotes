<%= form_with(model: quote) do |form| %>

  
  <% if quote.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
        <% quote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= form.label :Quote, class: 'control-label' %>
      <div class="controls">
    <%= form.text_field :quote_content %>
    </div>
  </div>

  <div class="control-group">
    <%= form.label :Author, class: 'control-label' %>
    <div class="controls">
    <%= form.text_field :quote_author %>
    </div>
  </div>

<br>
  <div>
  <div class="controls"
    <%= form.submit %>
  </div>
<% end %>

<div class="center">
  <h1>Create a Auote</h1>
  <form>
    <div class="inputbox">
      <input type="text" required="required">
      <span>Quote</span>
    </div>
    <div class="inputbox">
      <input type="text" required="required">
      <span>AUthor</span>
    </div>
    <div class="inputbox">
      <input type="button" value="submit">
    </div>
  </form>
</div>
